[project]
name = "sac-uto-touren-scraper"
version = "0.1.0"
description = "Scraper for sac-uto.ch tours, initially for morph.io"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["beautifulsoup4<4.11","requests"]
